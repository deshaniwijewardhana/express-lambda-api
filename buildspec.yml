version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 14
  build:
    commands:
      - echo "Building the project"
  post_build:
    commands:
      - echo "Build complete"
artifacts:
  files:
    - '**/*'
